#include<stdio.h>
int main(){
int i,n,a[10],key=0;
printf("enter the no.of elements");
scanf("%d",&n);
printf("enter the elements to be inserted");
for(int i=0;i<n;i++){
scanf("%d",&a);
}
for(inti=0;i<n;i++){
key=arr[i];
j=i-1;
while(j>=0 && a[j]>key){
            arr[j + 1] = arr[j];
            j = j - 1;
        }
        arr[j + 1] = key;
    }}
 for (i = 0; i < n; i++){
        cout << arr[i] << " ";
        }
    cout << endl;
return 0;
}
